# Example environment variables (do NOT commit secrets)
# Copy to .env locally and fill values before running in production

WIDOWBLUE_JWT_SECRET=replace-with-a-strong-random-string
NODE_ENV=production
# If you attach a Persistent Disk on Render, set the DB path accordingly:
WIDOWBLUE_DB_PATH=/data/widowblue.db
